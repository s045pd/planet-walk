# Three.js 星球漫步/行星探索 Web 项目竞品调研报告

## 1. 现有类似项目调研 (GitHub & Open Source)

在 GitHub 及开源社区中，基于 Three.js 的星球类项目主要分为三类：**太阳系科普展示型**、**程序化生成型**和**数据可视化型**。真正的“地表漫步”类 Web 项目相对较少，多为简单的 Demo。

### 1.1 代表性项目

| 项目名称/类型 | 特点描述 | 优缺点分析 |
| :--- | :--- | :--- |
| **Solar System (各种版本)** | **特点**：模拟太阳系八大行星轨道、自转、基本纹理。<br>**代表库**：`dataarts/3d-solar-system` (Google), `s-light/solar-system-threejs` | **优点**：宏观视角震撼，适合科普。<br>**缺点**：通常无法着陆，贴图精度低，近距离观察会模糊，缺乏交互深度。 |
| **Globe.gl** | **特点**：专注于地球的数据可视化，支持在球体上绘制点、线、热力图。<br>**技术**：Three.js + GeoJSON。 | **优点**：数据展示能力极强，性能优化好。<br>**缺点**：主要用于宏观数据分析，不支持地形起伏（Heightmap支持有限），不支持第一人称漫游。 |
| **Procedural Planet Generators** | **特点**：使用 Perlin Noise 或 Simplex Noise 在 Shader 中生成地形。<br>**代表库**：`seb776/planet-generator` | **优点**：无限细节，无需加载庞大的纹理文件。<br>**缺点**：计算量大，对 GPU 要求高，难以还原真实的星球地貌（如真实的火星奥林帕斯山）。 |
| **Mars Walk / Terrain Demos** | **特点**：加载局部的高精度地形模型（GLTF/DTM），实现第一人称控制。<br>**案例**：NASA 的 "Experience Curiosity" (非纯开源，但有类似 WebGL 实现)。 | **优点**：沉浸感强，有“漫步”的感觉。<br>**缺点**：通常只能在一个限定的方形区域移动，无法实现“环球”漫游（由于坐标系和精度问题）。 |

### 1.2 竞品痛点总结
*   **视角限制**：绝大多数项目仅支持轨道控制器（OrbitControls），即“上帝视角”，缺乏“人类视角”。
*   **精度与性能矛盾**：高精度纹理加载慢，程序化生成计算重。
*   **缺乏游戏性**：大多是静态展示，缺乏探索、采集、建造等互动机制。

---

## 2. 星球数据 API 与地形数据源

要实现真实的星球漫步，需要依赖高质量的纹理（Texture）、高度图（Heightmap/Displacement Map）和星历数据。

### 2.1 NASA 官方数据源
*   **NASA Visible Earth**: 最核心的纹理来源。提供地球各月份的“Blue Marble”图像，以及夜景、云层图。
    *   *用途*：基础漫反射贴图 (Diffuse Map)。
*   **NASA PDS (Planetary Data System)**: 包含火星（MGS）、月球（LRO）的详细地形数据。
    *   *用途*：生成真实的高程模型。
*   **NASA Open APIs**:
    *   **EPIC API**: 地球多色成像相机，提供每日地球全貌图。
    *   **NeoWs (Near Earth Object Web Service)**: 近地小行星数据。
    *   **Exoplanet Archive**: 系外行星数据（用于生成虚构宇宙）。

### 2.2 第三方与处理后的数据
*   **USGS (美国地质调查局)**: 提供 Astrogeology Science Center 数据，包含极高精度的月球和火星 DEM (数字高程模型)。
*   **Solar System Scope**: 提供了一套处理好的、适合 Web 渲染的 2k/4k/8k 行星纹理（包含颜色、法线、高光贴图）。
*   **Celestia Motherlode**: 老牌天文模拟软件的资源库，拥有大量社区制作的高清纹理。

---

## 3. 技术可行性分析

### 3.1 核心技术栈
*   **渲染引擎**: Three.js (r150+)。
*   **构建工具**: Vite (处理大型纹理资源更高效)。
*   **物理引擎**: Cannon-es 或 Rapier (用于处理漫步时的碰撞检测和重力)。

### 3.2 关键技术难点与解决方案

#### A. 坐标系与重力 (Spherical Gravity)
在球体表面漫步不同于平面游戏。
*   **难点**：Three.js 默认坐标系是 XYZ 笛卡尔坐标，角色在球体背面时头需要朝下。
*   **方案**：
    1.  禁用默认的重力方向。
    2.  每帧计算角色与球心的向量，施加向心力。
    3.  使用四元数 (Quaternion) 将角色的 `up` 向量对齐到球体法线方向。

#### B. 地形细节 (LOD - Level of Detail)
无法一次性加载整个星球的高精度模型。
*   **方案**：**QuadTree Sphere (四叉树球体)** 算法。
    *   距离远时：渲染简单的球体 Mesh。
    *   距离近时：将球面的局部区域细分为更高面数的网格，并应用更高分辨率的高度图。
    *   *参考技术*：Google Earth 的渲染逻辑。

#### C. 大气层渲染 (Atmosphere Scattering)
为了真实感，不能只贴一张图，需要体积光效果。
*   **方案**：使用自定义 Shader 实现 **Rayleigh Scattering (瑞利散射)** 和 **Mie Scattering (米氏散射)**。
    *   *现成方案*：`three-atmosphere-material` 或参考 Sean O'Neil 的经典算法。

#### D. 浮点数精度问题 (Z-fighting / Jitter)
当场景极大（星球尺度）且观察者极小（人）时，WebGL 会出现精度抖动。
*   **方案**：使用 **Logarithmic Depth Buffer (对数深度缓存)**，在 Three.js 渲染器配置中开启 `logarithmicDepthBuffer: true`。

---

## 4. 差异化机会 (Gap Analysis)

基于以上调研，开发一个新的“星球漫步”项目存在以下差异化切入点：

### 4.1 从“看”到“玩” (Gamification)
*   **现有**：点击星球 -> 缩放 -> 旋转查看。
*   **机会**：**第一人称着陆 (Seamless Landing)**。从太空无缝降落到地表，切换为 FPS/TPS 视角，驾驶漫游车（Rover）或步行。加入“采集样本”、“建立信标”等轻游戏元素。

### 4.2 虚实结合 (Procedural + Real Data)
*   **现有**：要么全是假的（程序化噪点），要么全是死的（静态卫星图）。
*   **机会**：**混合生成**。宏观使用 NASA 真实数据，微观（脚下 10 米范围）使用程序化生成的岩石、植被（InstancedMesh）填充细节。解决卫星图拉近后“马赛克”的问题。

### 4.3 多人在线探索 (Social Exploration)
*   **现有**：单机体验。
*   **机会**：引入 WebSocket。用户可以看到其他用户在星球表面的位置（类似《无人深空》的简化版），并在地表留下永久性的标记（如插旗、留言板）。

### 4.4 风格化渲染 (Stylized/Low Poly)
*   **现有**：追求极致写实但受限于 Web 性能，导致“恐怖谷”效应（看起来假）。
*   **机会**：采用 **Low Poly (低多边形)** 或 **Synthwave (合成波)** 风格。降低对纹理精度的依赖，侧重于光影、色彩和氛围感，性能更好且视觉风格独特。

## 5. 结论

技术上，使用 Three.js 实现“星球漫步”是完全可行的，但难点在于**动态地形细分 (LOD)** 和 **球体物理控制**。

**推荐 MVP (最小可行性产品) 路径**：
1.  不追求 1:1 真实比例，缩小星球半径（如 1km 半径）。
2.  使用 NASA 纹理做远景。
3.  实现平滑的球体引力控制器。
4.  在地表生成随机的岩石和兴趣点。
5.  核心体验：从太空无缝降落到地表并开始行走的 30 秒体验。
